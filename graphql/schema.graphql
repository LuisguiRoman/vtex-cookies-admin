type CookieFortune {
  id: ID!
  CookieFortune: String
}

type ResponseCreate {
  id: ID!
  phrase: String
}

type ResponseDeleted {
  data: String
  status: Int
  id: ID
}

type Query {
  getFortuneCookies: [CookieFortune]!
}

type Mutation {
  addFortuneCookie(phrase: String!): ResponseCreate!
  deleteFortuneCookie(id: ID!): ResponseDeleted!
}