mutation DeleteCookieFortune(
  $acronym: String!
  $documentId: ID!
) {
  deleteDocument(
    acronym: $acronym
    documentId: $documentId
  ) @context(provider: "vtex.store-graphql") {
    documentId
  }
}